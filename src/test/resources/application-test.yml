spring:
  datasource:
    url: jdbc:postgresql://${database.host}:${database.port}/recommender
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true

database:
  host: ${psql.host}
  port: ${psql.tcp.5432}